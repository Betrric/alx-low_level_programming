#include<stdio.h>
/*
 * main - A program that uses the function of printf
 * Return:0 if success
 */

int main(0)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
